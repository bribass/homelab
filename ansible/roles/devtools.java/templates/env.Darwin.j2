#!/bin/zsh

JAVA_HOME={{ java_dir }}/{{ java_type }}-{{ java_unpack_toplevel }}{{ java_env_toplevel }}
export JAVA_HOME

PATH=${JAVA_HOME}/bin:${PATH}
export PATH

if [ "X${MANPATH}" = "X" ]; then
  MANPATH=${JAVA_HOME}/man
else
  MANPATH=${JAVA_HOME}/man:${MANPATH}
fi
export MANPATH
